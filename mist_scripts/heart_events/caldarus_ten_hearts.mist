

wait(1);
fade_out(2);
scene(deep_woods);
place_actor(ari, north, c10h_ari_entrance);
place_actor(caldarus, north, c10h_caldarus_entrance);
camera_position(c10h_camera_1);
request_music_play("Music/Events/SharedLight");
sim {
    {
        wait(0.25);
        fade_in(2);
    }
    {
        pan_camera(3, c10h_camera_2);
    }
}
wait(1);
next_line(); //
sim {
    {
        wait(0.5);
        face(caldarus, east);
    }
    {
        wait(1);
        face(ari, west);
    }
    {
        next_line(); //
    }
}
close_textbox();
wait(0.5);
free bark_speech(ari, cute_face);
free bark_speech(caldarus, cute_face);
wait(2);
face(caldarus, north);
wait(0.5);
face(ari, north);
sim {
    {
        free pan_camera(4, c10h_camera_3);
    }
    {
        next_line(); //
        next_line(); //
        next_line(); //
        next_line(); //
    }
}

close_textbox();
free pan_camera(3, c10h_camera_2);
free bark_speech(caldarus, exclamation_mark);
wait(1.5);
face(ari, west);
wait(0.5);
face(caldarus, east);
wait(1);
next_line(); //
//
if get_response() == 0 {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(caldarus, blush);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
} else {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(caldarus, heart);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
}
close_textbox();
wait(0.5);
free bark_speech(caldarus, cute_face);
wait(1);
free bark_speech(ari, cute_face);
wait(2);
face(caldarus, north);
wait(1);
free bark_speech(caldarus, ellipses);
wait(2);
next_line(); //
next_line(); //
next_line(); //
close_textbox();
sim {
    {
        wait(1);
        face(caldarus, east);
    }
    {
        wait(2);
        next_line(); //
    }
}

close_textbox();
wait(0.5);
free bark_speech(ari, cute_face);
wait(1);
free bark_speech(caldarus, blush);
wait(1);
face(caldarus, west);
wait(1);
free pan_camera(2, c10h_camera_6);
wait(0.75);
sim {
    walk(caldarus, c10h_caldarus_start);
    set_move_speed(caldarus, 0.4);
}
face(caldarus, west);
wait(1.25);
next_line(); //
next_line(); //
sim {
    {
        wait(0.5);
        face(caldarus, east);
    }
    {
        next_line(); //
    }
}
close_textbox();
sim {
    {
        wait(1);
        walk_slow(caldarus, c10h_caldarus_entrance);
        face(caldarus, east);
    }
    {
        pan_camera(2, c10h_camera_5);
    }
    {
        wait(2);
        next_line(); //
    }
}
//
close_textbox();
sim {
    {
        wait(0.75);
        free bark_speech(caldarus, exclamation_mark);
    }
    {
        wait(1.5);
        next_line(); //
        next_line(); //
    }
}
close_textbox();
wait(0.5);
free pan_camera(2, c10h_camera_7);
wait(1);
face(caldarus, south);
wait(1);
next_line(); //
next_line(); //
close_textbox();
free pan_camera(2, c10h_camera_5);
wait(1);
face(caldarus, east);
wait(1);
next_line(); //
next_line(); //
close_textbox();
wait(0.25);
sim {
    {
        pan_camera(2, c10h_camera_4);
    }
    {
        wait(0.75);
        sim {
            walk(caldarus, c10h_caldarus_kiss);
            set_move_speed(caldarus, 0.4);
        }
        face(caldarus, east);
    }
    {
        wait(2);
        next_line(); //
    }
}
close_textbox();
free bark_speech(ari, exclamation_mark);
wait(2);

if read_world_fact("cutscene_seen_caldarus_ten_hearts") == true {
    next_line(); //
    next_line(); //
    next_line(); //
    close_textbox();
    wait(0.5);
    face(caldarus, north);
    wait(1);
    next_line(); //

} else {
    next_line(); //
    sim {
        {
            wait(1);
            animate(caldarus, action);
            wait(0.5);
            animate(caldarus, idle);
        }
        {
            wait(1.1);
            animate(ari, action);
            wait(0.5);
            animate(ari, idle);
        }
        {
            wait(2);
            ari_hold_item(caldarus_dragonsworn_statue);
        }
        {
            next_line(); //
            next_line(); //
        }
    }
    close_textbox();
    wait(0.5);
    face(caldarus, north);
    wait(1);
    next_line(); //
}
close_textbox();
wait(0.5);
free bark_speech(caldarus, blush);
wait(1);
free bark_speech(ari, cute_face);
wait(1);
face(caldarus, east);
wait(1);
free bark_speech(caldarus, cute_face);
wait(2);
sim {
    {
        wait(1);
        face(caldarus, south);
    }
    {
        next_line(); //
    }
}
sim {
    {
        wait(1);
        face(caldarus, east);
    }
    {
        next_line(); //
    }
}
close_textbox();
wait(0.5);
free bark_speech(ari, heart);
wait(1);
free bark_speech(caldarus, heart);
wait(2);
free bark_speech(caldarus, blush);
wait(1);
sim {
    {
        wait(1);
        face(caldarus, west);
    }
    {
        next_line(); //
    }
}
sim {
    {
        wait(0.5);
        face(caldarus, east);
    }
    {
        next_line(); //
    }
}

//
if get_response() == 0 {
    //
    close_textbox();
    sim {
        {
            wait(1);
            free bark_speech(caldarus, exclamation_mark);
        }
        {
            pan_camera(1.5, c10h_camera_5);
        }
        {
            wait(0.5);
            walk_slow(ari, c10h_ari_kiss);
            face(ari, west);
        }
        {
            wait(1.5);
            next_line(); //
            make_next_prompt_pink(0);
            next_line(); //
        }
    }
    //
    if get_response() == 0 {
        set_on_blackboard("trigger_proposal_with", "caldarus");
        close_textbox();
        wait(0.5);
        free bark_speech(caldarus, exclamation_mark);
        wait(1);
        checkpoint("kiss");
        next_line(); //
        next_line(); //
        next_line(); //
        next_line(); //
        close_textbox();
        wait(0.5);
        kiss_npc(caldarus, true, 1);
        wait(2);
        next_line(); //
        next_line(); //
        next_line(); //
        next_line(); //
        close_textbox();
        wait(0.5);
        free bark_speech(caldarus, heart);
        wait(0.5);
        free bark_speech(ari, heart);
        fade_out(2);
    } else {
        next_line(); //
        next_line(); //
        close_textbox();
        wait(0.5);
        free bark_speech(caldarus, cute_face);
        wait(1);
        free bark_speech(ari, cute_face);
        fade_out(2);
    }
} else {
    next_line(); //
    next_line(); //
    close_textbox();
    wait(0.5);
    free bark_speech(caldarus, cute_face);
    wait(1);
    free bark_speech(ari, cute_face);
    fade_out(2);
}
