wait(1);
fade_out(2);
scene(summit);
place_actor(ari, north, j10h_ari_entrance);
place_actor(juniper, north, j10h_juniper_entrance);
camera_position(j10h_camera_1);
request_music_play("Music/Events/SharedLight");
sim {
    {
        wait(0.5);
        fade_in(3);
    }
    {
        pan_camera(4, j10h_camera_2);
    }
}
wait(1);
next_line(); //
sim {
    {
        wait(0.5);
        face(juniper, east);
    }
    {
        wait(1);
        face(ari, west);
    }
    {
        next_line(); //
    }
}
close_textbox();
wait(0.5);
free bark_speech(ari, cute_face);
free bark_speech(juniper, cute_face);
wait(2);
face(juniper, north);
wait(0.5);
face(ari, north);
pan_camera(4, j10h_camera_3);
next_line(); //
next_line(); //
close_textbox();
free pan_camera(3, j10h_camera_2);
wait(2);
face(juniper, east);
wait(0.5);
face(ari, west);
wait(0.5);
next_line(); //
sim {
    {
        wait(0.5);
        face(juniper, north);
    }
    {
        next_line(); //
        next_line(); //
    }
}


//

if get_response() == 0 {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(juniper, blush);
        }
        {
            wait(1.25);
            face(juniper, east);
        }
        {
            wait(1.5);
            next_line(); //
            next_line(); //
        }
    }
} else {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(juniper, cute_face);
        }
        {
            wait(1.25);
            face(juniper, east);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
}
close_textbox();
wait(0.5);
free bark_speech(juniper, cute_face);
wait(1);
free bark_speech(ari, cute_face);
wait(2);
face(juniper, north);
wait(1);
face(ari, north);
wait(0.5);
free bark_speech(juniper, ellipses);
pan_camera(4, j10h_camera_3);
next_line(); //
next_line(); //
next_line(); //
next_line(); //
close_textbox();
free pan_camera(3, j10h_camera_2);
wait(2);
face(juniper, east);
wait(0.5);
face(ari, west);
wait(0.5);
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
free bark_speech(ari, cute_face);
wait(1.5);
free bark_speech(juniper, ellipses);
wait(1);
free pan_camera(2, j10h_camera_4);
face(juniper, west);
wait(1);
sim {
    walk(juniper, j10h_juniper_west);
    set_move_speed(juniper, 0.4);
}
face(juniper, west);
next_line(); //
next_line(); //

//
if get_response() == 0 {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(juniper, blush);
        }
        {
            wait(1.5);
            face(juniper, east);
        }
        {
            wait(2);
            next_line(); //
        }
    }
} else {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(juniper, exclamation_mark);
        }
        {
            wait(1.25);
            face(juniper, east);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
}
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
free bark_speech(ari, cute_face);
wait(1);
free bark_speech(juniper, cute_face);
wait(2);
face(juniper, west);
wait(0.5);
next_line(); //
next_line(); //
sim {
    {
        wait(0.5);
        face(juniper, east);
    }
    {
        next_line(); //
    }
}
sim {
    {
        wait(0.5);
        face(juniper, west);
    }
    {
        next_line(); //
    }
}
close_textbox();
wait(1);
face(juniper, east);
sim {
    {
        wait(0.5);
        sim {
            walk(juniper, j10h_juniper_kiss);
            set_move_speed(juniper, 0.4);
        }
        face(juniper, east);
    }
    {
        pan_camera(2, j10h_camera_5);
    }
    {
        wait(2);
        next_line(); //
    }
}
close_textbox();
wait(0.25);
free bark_speech(ari, exclamation_mark);
wait(2);

if read_world_fact("cutscene_seen_juniper_ten_hearts") == true {
    next_line(); //
    next_line(); //
    next_line(); //
} else {
    sim {
        {
            wait(1);
            animate(juniper, action);
            wait(0.1);
            animate(ari, action);
            wait(0.4);
            animate(juniper, idle);
            wait(0.1);
            animate(ari, idle);
        }
        {
            wait(2);
            ari_hold_item(junipers_protection_scroll);
        }
        {
            next_line(); //
            next_line(); //
            next_line(); //
        }
    }
}
close_textbox();
wait(0.5);
free bark_speech(ari, blush);
wait(1);
free bark_speech(juniper, cute_face);
wait(2);
next_line(); //
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
free bark_speech(juniper, heart);
wait(1);
free bark_speech(ari, heart);
wait(1);
free bark_speech(juniper, blush);
wait(0.5);
face(juniper, north);
wait(1);
next_line(); //

//
if get_response() == 0 {
    //
    close_textbox();
    sim {
        {
            wait(0.5);
            walk_slow(ari, j10h_ari_kiss);
            face(ari, west);
        }
        {
            pan_camera(1.5, j10h_camera_2);
        }
        {
            wait(1);
            free bark_speech(juniper, exclamation_mark);
        }
        {
            wait(1.5);
            face(juniper, east);
        }
        {
            wait(2);
            next_line(); //
            make_next_prompt_pink(0);
            next_line(); //
        }
    }
    //
    if get_response() == 0 {
        set_on_blackboard("trigger_proposal_with", "juniper");
        close_textbox();
        wait(0.5);
        free bark_speech(juniper, exclamation_mark);
        wait(1.25);
        next_line(); //
        next_line(); //
        next_line(); //
        close_textbox();
        wait(0.5);
        checkpoint("kiss");
        sim {
            {
                face(juniper, south);
                animate(juniper, laugh);
                wait(2);
            }
            {
                play_sound("SoundEffects/NPCs/Vocal/JuniperLaugh");
            }
            {
                wait(0.25);
                next_line(); //
                close_textbox();
            }
        }
        wait(0.5);
        animate(juniper, idle);
        face(juniper, east);
        wait(0.5);
        kiss_npc(juniper, true, 1);
        wait(2);
        next_line(); //
        next_line(); //
        next_line(); //
        next_line(); //
        close_textbox();
        wait(0.5);
        free bark_speech(juniper, heart);
        wait(0.5);
        free bark_speech(ari, heart);
        fade_out(2);
    } else {
        sim {
            {
                wait(1);
                face(juniper, east);
            }
            {
                next_line(); //
                next_line(); //
            }
        }
        close_textbox();
        wait(0.5);
        free bark_speech(juniper, cute_face);
        wait(1);
        free bark_speech(ari, cute_face);
        fade_out(2);
    }
} else {
    sim {
        {
            wait(1);
            face(juniper, east);
        }
        {
            next_line(); //
            next_line(); //
        }
    }
    close_textbox();
    wait(0.5);
    free bark_speech(juniper, cute_face);
    wait(1);
    free bark_speech(ari, cute_face);
    fade_out(2);
}
