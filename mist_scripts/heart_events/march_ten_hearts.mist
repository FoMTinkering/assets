wait(1);
fade_out(2);
scene(summit);
place_actor(ari, north, m10h_ari_entrance);
place_actor(march, north, m10h_march_entrance);
camera_position(m10h_camera_1);
request_music_play("Music/Events/SharedLight");


sim {
    {
        wait(0.25);
        fade_in(2);
    }
    {
        pan_camera(4, m10h_camera_2);
    }
}
wait(1);
next_line(); //
sim {
    {
        wait(0.5);
        face(march, east);
    }
    {
        wait(1);
        face(ari, west);
    }
    {
        next_line(); //
    }
}
close_textbox();
wait(0.5);
free bark_speech(march, cute_face);
wait(1);
free bark_speech(ari, cute_face);
wait(1.5);
face(march, north);
sim {
    {
        wait(0.5);
        walk_slow(march, m10h_march_start);
        face(march, north);
    }
    {
        pan_camera(4, m10h_camera_3);
    }
    {
        wait(1);
        face(ari, north);
    }
}
next_line(); //
next_line(); //
close_textbox();
wait(1);
free bark_speech(march, ellipses);
wait(0.5);
sim {
    {
        wait(1.5);
        walk_slow(march, m10h_march_entrance);
        face(march, east);
    }
    {
        wait(2.25);
        face(ari, west);
    }
    {
        pan_camera(3, m10h_camera_2);
    }
    {
        wait(3);
        next_line(); //
        next_line(); //
    }
}
sim {
    {
        wait(0.5);
        face(march, west);
    }
    {
        next_line(); //
    }
}
sim {
    {
        wait(0.5);
        face(march, east);
    }
    {
        next_line(); //
    }
}
//
if get_response() == 0 {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(march, exclamation_mark);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
    sim {
        {
            wait(1);
            face(march, west);
        }
        {
            next_line(); //
        }
    }
    sim {
        {
            wait(0.5);
            face(march, east);
        }
        {
            next_line(); //
            next_line(); //
        }
    }
} else {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(march, blush);
        }
        {
            wait(1.75);
            face(march, west);
        }
        {
            wait(2);
            next_line(); //
        }
    }
    sim {
        {
            wait(1);
            face(march, east);
        }
        {
            next_line(); //
            next_line(); //
            next_line(); //
        }
    }
}
close_textbox();
wait(0.5);
face(march, west);
wait(1);
next_line(); //
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
face(march, east);
wait(1);
next_line(); //
close_textbox();
wait(0.5);
free bark_speech(march, cute_face);
wait(1);
free bark_speech(ari, cute_face);
wait(2);
face(march, north);
wait(0.5);
face(ari, north);
pan_camera(4, m10h_camera_3);
next_line(); //
next_line(); //
sim {
    {
        wait(0.5);
        face(ari, west);
    }
    {
        next_line(); //
    }
}

close_textbox();
wait(0.5);
free pan_camera(3, m10h_camera_2);
wait(1);
free bark_speech(march, ellipses);
wait(2);
sim {
    {
        wait(0.5);
        face(march, east);
    }
    {
        next_line(); //
        next_line(); //
    }
}

//
if get_response() == 0 {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(march, blush);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
} else {
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(march, exclamation_mark);
        }
        {
            wait(1.5);
            next_line(); //
        }
    }
}
close_textbox();
wait(0.5);
free bark_speech(march, cute_face);
wait(0.5);
free bark_speech(ari, cute_face);
wait(1.5);
face(march, north);
wait(0.5);
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
face(march, west);
free pan_camera(2, m10h_camera_5);
wait(1);
sim {
    walk(march, m10h_march_west);
    set_move_speed(march, 0.4);
}
face(march, west);
next_line(); //
close_textbox();
wait(0.5);
free bark_speech(march, ellipses);
wait(2);

sim {
    {
        wait(0.5);
        face(march, east);
    }
    {
        next_line(); //
        next_line(); //
        next_line(); //
    }
}
close_textbox();
wait(0.25);
sim {
    {
        pan_camera(2, m10h_camera_4);
    }
    {
        wait(0.5);
        sim {
            walk(march, m10h_march_kiss);
            set_move_speed(march, 0.4);
        }
        face(march, east);
    }
    {
        wait(2);
        next_line(); //
    }
}
close_textbox();
wait(0.25);
free bark_speech(ari, exclamation_mark);
wait(1.5);
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
free bark_speech(ari, cute_face);
wait(1);
free bark_speech(march, blush);
wait(1);
face(march, west);
wait(1);
next_line(); //
sim {
    {
        wait(0.5);
        face(march, east);
    }
    {
        next_line(); //
    }
}


if read_world_fact("cutscene_seen_march_ten_hearts") == true {
    next_line(); //
    next_line(); //
} else {
    sim {
        {
            wait(1);
            animate(march, action);
            wait(0.1);
            animate(ari, action);
            wait(0.4);
            animate(march, idle);
            wait(0.1);
            animate(ari, idle);
        }
        {
            wait(2);
            ari_hold_item(marchs_mistrian_shield);
        }
        {
            next_line(); //
            next_line(); //
        }
    }
}
close_textbox();
wait(0.5);
free bark_speech(march, heart);
wait(0.5);
free bark_speech(ari, heart);
wait(1.5);
next_line(); //
next_line(); //
close_textbox();
wait(1);
face(march, west);
wait(1);
sim {
    walk(march, m10h_march_entrance);
    set_move_speed(march, 0.4);
}
face(march, west);
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
face(march, east);
wait(0.5);
next_line(); //
next_line(); //


//
//
if get_response() == 0 {
    //
    close_textbox();
    sim {
        {
            wait(0.75);
            free bark_speech(march, exclamation_mark);
        }
        {
            wait(1.25);
            next_line(); //
            next_line(); //
            next_line(); //
        }
    }
    sim {
        {
            sim {
                walk(march, m10h_march_kiss);
                set_move_speed(march, 0.4);
            }
            face(march, east);
        }
        {
            make_next_prompt_pink(0);
            next_line(); //
        }
    }
    //
    if get_response() == 0 {
        set_on_blackboard("trigger_proposal_with", "march");
        close_textbox();
        wait(0.25);
        free pan_camera(1.5, m10h_camera_2);
        wait(0.5);
        walk_slow(ari, m10h_ari_kiss);
        face(ari, west);
        wait(0.25);
        free bark_speech(march, exclamation_mark);
        wait(1);
        checkpoint("kiss");
        next_line(); //
        next_line(); //
        next_line(); //
        next_line(); //
        close_textbox();
        wait(0.5);
        kiss_npc(march, true, 1);
        wait(2);
        next_line(); //
        sim {
            {
                wait(0.5);
                sim {
                    walk(march, m10h_march_entrance);
                    set_move_speed(march, 0.4);
                }
                face(march, west);
            }
            {
                next_line(); //
                next_line(); //
            }
        }
        sim {
            {
                wait(0.5);
                face(march, east);
                wait(1);
                sim{
                    walk(march, m10h_march_kiss);
                    set_move_speed(march, 0.4);
                }
                face(march, east);
            }
            {
                next_line(); //

            }
        }
        next_line(); //
        close_textbox();
        wait(0.5);
        free bark_speech(march, heart);
        wait(0.5);
        free bark_speech(ari, heart);
        fade_out(2);
    } else {
        next_line(); //
        sim {
            {
                wait(0.5);
                face(march, west);
            }
            {
                next_line(); //
            }
        }
        sim {
            {
                wait(0.5);
                face(march, east);
            }
            {
                next_line(); //
            }
        }
        close_textbox();
        wait(0.5);
        free bark_speech(march, cute_face);
        wait(1);
        free bark_speech(ari, cute_face);
        fade_out(2);
    }
} else {
    next_line(); //
    next_line(); //
    close_textbox();
    wait(0.5);
    free bark_speech(march, cute_face);
    wait(1);
    free bark_speech(ari, cute_face);
    fade_out(2);
}
