scene(seridias_chamber);
place_actor(seridia, south, crystal);
set_on_blackboard("block_skip", true);

//
next_line(); //
next_line(); //
next_line(); //
close_textbox();
set_on_blackboard("block_skip", false);

sim {
	//
	//
	//
	//
	{
		walk_slow(ari, dt2_ari_entrance);
		face(ari, north);
	}
	{
		pan_camera(3, dt2_camera_1);
	}
}
free bark(seridia, question_mark);
wait(1);
free bark_speech(ari, ellipses);
wait(2);
next_line(); //
next_line(); //
next_line(); //
close_textbox();
wait(1);
animate(seridia, blink);
wait(0.1);
anim_speed(seridia, 0);
wait(0.5);
next_line(); //
close_textbox();
wait(1);

sim {
	{
		wait(0.5);
		anim_speed(seridia, 1);
		animate(seridia, idle);
	}
	{
		next_line(); //
	}
}
next_line(); //
//
sim {
	{
		wait(0.75);
		free bark(seridia, exclamation_mark);
	}
	{
		next_line(); //
	}
}
close_textbox();
wait(0.5);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, mining);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, annoyed);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, eiland);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, archaeology);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, juniper);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, plant_tonic);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, book);
wait(1);
set_on_blackboard("next_bark_sfx", "SoundEffects/Barks/BubbleNeutral");
free bark_speech(ari, cute_face);
wait(1);
next_line(); //
next_line(); //
next_line(); //
next_line(); //
next_line(); //
//
sim {
	{
		wait(0.75);
		free bark(seridia, exclamation_mark);
	}
	{
		next_line(); //
	}
}
close_textbox();
wait(1);
free bark(seridia, ellipses);
wait(2);
next_line(); //
close_textbox();
wait(0.5);
sim {
	{
		wait(0.5);
		walk_slow(ari, dt2_ari_start);
		face(ari, north);
		animate(ari, action);
		wait(0.2);
		freeze_ari();
	}
	{
		pan_camera(2.5, dt2_camera_2);
	}
}
wait(0.5);
animate(seridia, blink);
wait(0.1);
anim_speed(seridia, 0);
play_sound("SoundEffects/NPCs/SeridiaMagicSimple");
wait(2);
free bark(seridia, ellipses);
wait(2);
next_line(); //
if read_world_fact("eiland_status") != "undefined" and read_world_fact("juniper_status") != "undefined" {

} else {
	next_line(); //
}
close_textbox();
wait(0.5);
free bark_speech(ari, exclamation_mark);
wait(1);
animate(ari, idle);
wait(0.5);
sim {
	{
		walk_slow(ari, dt2_ari_entrance);
		face(ari, north);
	}
	{
		wait(0.5);
		anim_speed(seridia, 1);
		animate(seridia, idle);
	}
	{
		pan_camera(2, dt2_camera_1);
	}
}
wait(0.5);
next_line(); //
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
if read_world_fact("eiland_status") != "undefined" and read_world_fact("juniper_status") != "undefined" {
	free bark_speech(ari, cute_face);
} else {
	free bark_speech(ari, ellipses);
}
wait(1.5);
next_line(); //
next_line(); //
close_textbox();
wait(0.5);
if read_world_fact("eiland_status") != "undefined" and read_world_fact("juniper_status") != "undefined" {
	free bark(seridia, cute_face);
	wait(1);
	bark_speech(ari, cute_face);
}
pan_camera(2, dt2_ari_entrance);
